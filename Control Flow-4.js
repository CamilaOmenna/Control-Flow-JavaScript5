const prompt = require('prompt sing')();

let número = parseInt(prompt('O numero  '));

if(numero % 4== 0) {

    numero = Math.sqrt(numero,8)
     console.log('Par')
     console.log(`${numero}`)
} else {
    
    number = Math.pow(numero,9)
    console.log('Impar')
    console.log(`${numero}`)
}
